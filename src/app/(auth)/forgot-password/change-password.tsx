import { View } from "react-native";
import { Text } from "@/components/ui/text";

export default function ChangePaswordScreen() {
	return (
		<View className="flex-1 justify-center items-center">
			<Text>Hi forgot password request</Text>
		</View>
	);
}
