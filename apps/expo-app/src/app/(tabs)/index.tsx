import { View } from "react-native";
import { Text } from "@/components/ui/text";

const Index = () => {
  return (
    <View className="flex-1 items-center justify-center">
      <Text variant={"h1"}>Index Screen</Text>
    </View>
  );
};

export default Index;
